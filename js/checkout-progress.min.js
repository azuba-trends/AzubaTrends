const CheckoutProgress=(function(){const e=["Cart","Checkout","Payment"];function o(r,c){r&&(r.innerHTML=`
      <div class="checkout-progress">
        ${e.map((n,s)=>{const t=s<c?"is-done":s===c?"is-current":"",a=s<e.length-1?`<div class="checkout-progress__line ${s<c?"is-done":""}"></div>`:"";return`
            <div class="checkout-progress__step ${t}">
              <span class="checkout-progress__circle">${s<c?"\u2713":s+1}</span>
              <span class="checkout-progress__label">${n}</span>
            </div>
            ${a}`}).join("")}
      </div>`)}return window.CheckoutProgress={render:o},{render:o}})();
