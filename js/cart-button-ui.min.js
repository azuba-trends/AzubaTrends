const CartButtonUI=(function(){function i(n){if(!window.Cart)return 0;const t=window.Cart.getItems().find(e=>e.productId===String(n));return t?t.quantity:0}function a(n,t){if(!n)return;const e=i(t.productId);if(t.stock===0){n.innerHTML='<button class="btn btn-outline btn-block" disabled>Out of Stock</button>';return}e>0?n.innerHTML=`
        <div class="qty-stepper" data-product-id="${t.productId}">
          <button type="button" class="qty-stepper__btn" data-qty-dec aria-label="Decrease quantity">\u2212</button>
          <span class="qty-stepper__value">${e}</span>
          <button type="button" class="qty-stepper__btn" data-qty-inc aria-label="Increase quantity">+</button>
        </div>`:n.innerHTML=`
        <button class="btn btn-accent btn-block"
          data-add-to-cart
          data-product-id="${t.productId}"
          data-product-title="${t.title}"
          data-product-price="${t.price}"
          data-product-image="${t.image}"
          data-product-size="${t.size||""}"
          data-product-color="${t.color||""}">
          Add to Cart
        </button>`}function o(n,t){!n||!t||typeof t.productId=="undefined"||(a(n,t),window.addEventListener("cart:updated",()=>a(n,t)),n.addEventListener("click",e=>{const r=e.target.closest("[data-qty-dec]"),u=e.target.closest("[data-qty-inc]");if(!r&&!u)return;e.preventDefault();const d=i(t.productId);d<=0||window.Cart.updateQuantity(t.productId,r?d-1:d+1)}))}return{mount:o,render:a}})();typeof window!="undefined"&&(window.CartButtonUI=CartButtonUI);
